export interface IBook {
    // Definimos una interfaz para el modelo de libro.
    id: number;    // El identificador único del libro.
    title: string; // El título del libro.
    author: string; // El autor del libro.
  }
  